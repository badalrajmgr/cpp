#include<iostream>
#include<unordered_map>
#include<vector>
using namespace std;
int main(){
    vector<int> v;
    unordered_map<int,int> m;
        // m[v[i]]++ frequency  increased as m[v[i]] is key and fixed 
        // m.first();
        // m.second();
    return 0;
}